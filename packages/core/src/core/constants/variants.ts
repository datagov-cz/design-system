export enum Variants {
	PRIMARY = 'primary',
	SECONDARY = 'secondary',
	SUCCESS = 'success',
	ERROR = 'error',
	WARNING = 'warning',
	CUSTOM = 'custom',
}

export enum Types {
	CLASSIC = 'classic',
	SOLID = 'solid',
}
